class Solution {
public:
    vector<vector<int>> shiftGrid(vector<vector<int>>& grid, int k) {
        int m = grid.size();
        int n = grid[0].size();
        int total = m * n;
        k %= total; // shifting by 'total' brings you back to start
        
        vector<vector<int>> result(m, vector<int>(n));
        
        for (int i = 0; i < m; i++) {
            for (int j = 0; j < n; j++) {
                int flatIdx = i * n + j;
                int newFlatIdx = (flatIdx + k) % total;
                result[newFlatIdx / n][newFlatIdx % n] = grid[i][j];
            }
        }
        return result;
    }
};
